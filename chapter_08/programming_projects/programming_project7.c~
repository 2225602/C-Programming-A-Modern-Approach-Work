#include <stdio.h>

int main(void)
{

  int matrix[5][5] = {0};

  for (int row = 0; row < 5; row++) {
    printf("Enter row %d: ", row);
    for (int column = 0; column < 5; column++) {
      int num; scanf("%d", &num);
      matrix[row][column] = num;
    }
  }
}
