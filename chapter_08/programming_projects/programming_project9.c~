#include <stdio.h>
#include <stdbool.h>

int main(void)
{
  char board[10][10];
  for (int i = 0; i < 10; i++)
    for (int j = 0; j < 10; j++)
      board[i][j] = '.';
  char checked_directions[4] = {false};
  
  
}
